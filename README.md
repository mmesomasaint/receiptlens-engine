# receiptlens-engine
An enterprise-grade, asynchronous document ingestion, OCR parsing, and vector semantic search engine engineered with Node.js (v20+ LTS), TypeScript, Express, Prisma ORM, PostgreSQL with pgvector, Redis, BullMQ, Google APIs (Gmail/Drive), and a dedicated Python FastAPI OCR/Embedding microservice (ocr-extractor).
